// If life was easy, we could just do things the easy way:
// var getElementsByClassName = function (className) {
//   return document.getElementsByClassName(className);
// };

// But instead we're going to implement it from scratch:
var getElementsByClassName = function(className) {
  // assign empty array to domFamily
  // declare lostChildren function
    // assign arguments parameter to someNode
    // if someNode's classList contains the className
      // push someNode into domArray
    // if someNode has children
      // call lostChildren on someNode's children

// invoke lostChildren on the document.body
// return domFamily
};
